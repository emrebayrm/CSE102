#include<stdio.h>
#include<time.h>
#include<stdlib.h>
#define SENTINELEX 'e' 'E'
#define SENTINLEPL 'p' 'P'
int RNG(void);

int main ()
{
	int guess;
	int number;
	number=RNG()%10+1
}

int RNG(void)
{
	int rand_num;
	srand((unsigned)time(NULL));
	rand_num=rand();
	return rand_num;
}

